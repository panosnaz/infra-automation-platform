---
type: adr
domain: cisco_aci
status: active
tags: [aci, domain-coverage, roadmap, generator, terraform, mcp]
owner: platform-engineering-team
last_updated: 2026-09-04
---

# ADR-020 — ACI Domain Coverage Expansion (Tenant Policy Depth, then Access/Fabric Policies)

**Status:** Accepted — all phases complete and live-verified.

## Summary (read this first — the Progress section below is a detailed, phase-by-phase implementation log)

**What this ADR is:** a plan (and now a complete record) of expanding *how much of real ACI configuration* this platform can manage, beyond the original Tenant/VRF/Bridge-Domain basics. Three phases, each reusing the exact same pipeline mechanism (no new pipeline stages, ever — only new Terraform resources and generator logic):

- **Phase A — Tenant Policy Depth**: VRF/Bridge-Domain attribute depth, Application Profiles/EPGs, Contracts/Filters/Subjects, and L3Out (external connectivity). All 4 items complete and live-verified with real `terraform apply`+`destroy` cycles against the ACI simulator.
- **Phase B — Access Policies**: VLAN Pools, Physical Domains, AEPs, Leaf Interface Policy Groups — the fabric-wide objects that stage physical connectivity. Complete, but **logical-only**: this lab's ACI simulator has zero real leaf/spine interface data (confirmed directly via APIC API queries), so physical port/interface binding was never possible here — a permanent limitation of this specific simulator, not a gap in the design.
- **Phase C — Fabric Policies**: POD-wide NTP/DNS/SNMP configuration. Complete, with no physical-interface limitation (this is logical, fabric-wide config). Along the way, this phase caught and fixed a real, generalizable Terraform safety bug — see below.
- **Phase D — VMM Domain Integration**: VMware VMM Domain, Controller (vCenter association), and Credential objects. Complete and live-verified against this lab's real vCenter (`netvcenter.sc.gr`) and the real ACI simulator — no simulator/hardware limitation, unlike Phase B/Phase A item 4, since VMM Domain objects require no physical leaf/spine interface data at all. Followed by an EPG-to-Domain binding follow-on (Physical + VMM), also complete.
- **Phase E — Fabric Policies beyond NTP/DNS/SNMP**: COOP Group Policy, ISIS Domain Policy (both mandatory fabric-wide singletons, managed via real *typed* Terraform resources rather than `aci_rest_managed`), and named Pod Policy Groups (purely additive). Complete and live-verified. BGP Route Reflector node binding and Pod Selector/Pod Profile reassignment are explicitly out of scope for this pass — see the Phase E section for why.

**The one bug worth knowing about, even without reading the details:** `aci_rest_managed` (Terraform's generic "manage any ACI object" resource) deletes the entire target object on `terraform destroy` by default — which is dangerous when the target is a mandatory system object that can't actually be removed (like the fabric's default NTP/DNS/SNMP policies). This was caught live (a real destroy briefly deleted real fabric objects, then was recovered), and fixed by setting `content_on_destroy` to revert to safe defaults instead of deleting. Worth remembering for any future use of this resource type, or of any other provider's equivalent "manage anything" resource. **Real typed resources can behave differently and more safely** — Phase E found that `aci_coop_policy`/`aci_isis_domain_policy` refuse the underlying APIC delete outright (a provider-level warning, `"Resource with class name ... cannot be deleted"`), leaving the real object completely untouched on `terraform destroy`. Never assume this protection exists without testing it directly against the real target, the same way Phase C's incident proved it can't be assumed for `aci_rest_managed`.

**Everything in this ADR is done.** If you're looking for what's *not* done in ACI domain coverage, check [`Platform-Status-and-Pending-Items.md`](../architecture/Platform-Status-and-Pending-Items.md) instead of this file.

---

**Date:** 2026-07-29

**Decision Makers:** Platform Engineering Team

**Related ADRs:**

- ADR-002 — Terraform as the Declarative Provisioning Engine (this ADR extends Terraform's scope, not its role)
- ADR-007 — Cisco NetAsCode as the Canonical Engineering Model (the schema this expansion adds to)
- ADR-017 — Execution Framework (the pipeline this expansion reuses unchanged)
- ADR-018 — NetAsCode YAML as the Authoritative Intent Artifact (governs how new object types are added: through the generator, never a parallel schema)
- ADR-019 — Three Truths Principle (Desired State ownership boundary this expansion stays inside)

---

# Context

Execution Framework Milestones 1-5 (see [`Execution-Framework.md`](../architecture/Execution-Framework.md) §6) proved the full lifecycle — Nautobot → generator → GitLab CI (validate → policy → approve → apply → configure → verify → capture) → MCP Server — end to end, against live infrastructure. But the *domain automation* those milestones proved is narrow: checked directly against the code (`platform/python/generator/transformer.py`, `platform/terraform/aci/main.tf`), the only ACI object types under repeatable, Nautobot-driven management today are:

| Object | Status |
|---|---|
| Tenant | Full lifecycle |
| VRF | Full lifecycle |
| Bridge Domain | Full lifecycle (unicast routing only, no L2/L3 attribute depth) |
| Subnet (BD gateway) | Full lifecycle |

Everything else a real ACI deployment needs is either absent or exists only as a hand-scripted, non-repeatable example:

- **Application Profiles / EPGs** — `platform/ansible/aci/playbooks/day2-epg.yml` exists but hardcodes tenant/AP/EPG/BD names; it is not driven by Nautobot data and is not part of the pipeline.
- **Contracts / Filters / Subjects** — no code, no schema, no Nautobot modeling at all.
- **L3Outs / external connectivity** — no code, no schema, no Nautobot modeling at all.
- **Access Policies** (VLAN pools, physical/VMM domains, AEPs, leaf/spine interface policies, interface/leaf profiles) — no code. This is the layer that actually stages physical fabric connectivity (which ports carry which VLANs/EPGs).
- **Fabric Policies** (POD-wide NTP/DNS/SNMP, etc.) — no code.

This gap was flagged directly by the user: *"realistically, we need to support all kind of configurations and staging of the APIC and fabric"* — the current pipeline proves the *mechanism* (Milestones 1-5), not *coverage*.

A prior planning document, [`ACI-Nautobot-NaC-Architecture.md`](../architecture/ACI-Nautobot-NaC-Architecture.md), already contains a detailed NetAsCode YAML schema and Nautobot-object mapping for Application Profiles, EPGs (with physical/VMM domain bindings and static port bindings), VLAN Pools, physical/VMM Domains, AEPs, and leaf interface policy groups/profiles/selectors (its Parts 2, 5, and 6). That document's own "Implementation Phases" (Part 7) is superseded — it predates and describes a different, more primitive delivery mechanism (a manually-triggered Nautobot Job, no GitLab CI, no policy/approval gates) than the Execution Framework this repository actually built. Its schema and data-model design remain valid and are reused here; its delivery-mechanism plan is not. Neither Contracts/Filters/Subjects nor L3Outs are covered by that document at all — those need new design work, not just implementation of an existing plan.

---

# Decision

Expand ACI domain coverage in two sequenced phases, **both reusing the Execution Framework's existing machinery (generator pattern, GitLab CI pipeline stages, policy/approval gates, MCP Server tool registry) completely unchanged** — per ADR-018, a new object type is added by extending the generator's output schema and adding Terraform resources, never by inventing a new pipeline stage or a parallel intent schema.

## Phase A — Tenant Policy Depth

Extends the existing Tenant → VRF → Bridge Domain chain. Reuses Nautobot's current Tenant/VRF/Prefix data model plus Custom Fields (the same mechanism already used for `write_results.py`'s status fields) — no new Nautobot plugin or custom model required for the BD/VRF attribute depth; Application Profiles/EPGs need a small addition (see below).

1. **VRF and Bridge Domain attribute depth** — the four VRF-level and ten BD-level attributes already designed in `ACI-Nautobot-NaC-Architecture.md` Part 3 (e.g. `contract_enforcement_preference`, `unicast_routing` detail, `arp_flooding`, `l2_unknown_unicast`, `pim`), added as Nautobot Custom Fields on `VRF`/`Prefix` and read by the generator. Lowest-risk, highest-value first step — pure attribute enrichment of objects the generator already handles.
2. **Application Profiles + EPGs** — per the schema already designed in `ACI-Nautobot-NaC-Architecture.md` Part 5 (`apic.tenants[].application_profiles[].endpoint_groups[]`, including `bridge_domain` binding, `physical_domains`/`vmm_domains`, and `static_ports`). Requires modeling Application Profile and EPG in Nautobot — a lightweight custom model or Nautobot's `Tag`/`Relationship` primitives are sufficient (per that document's own "Plugin-managed custom model" note); does not require a new Nautobot plugin. New Terraform resources: `aci_application_profile`, `aci_epg`, `aci_epg_to_domain`, `aci_static_path` (or equivalent, per the netascode/aci provider's actual resource names — confirm against the provider docs before implementing, not assumed).
3. **Contracts / Filters / Subjects** — new design (not covered by the existing planning doc). Minimum viable schema: `apic.tenants[].contracts[].{name, scope, subjects[].{name, filters[]}}`, `apic.tenants[].filters[].{name, entries[].{ether_type, ip_protocol, ports}}`, plus EPG-level `provided_contracts[]`/`consumed_contracts[]`. Nautobot modeling: likely a custom model per Contract/Filter, or (simpler, deferred decision) representing them as structured Custom Field data on the Tenant until real usage proves whether first-class Nautobot objects are worth the modeling cost.
4. **L3Out** — new design, largest single item in Phase A (routing protocol config, external EPGs, L3Out interface profiles). Deferred to the end of Phase A; scope it properly (a short design note, not a full ADR) once EPG/Contract are proven, since L3Out's Nautobot modeling questions (which objects represent an external routed connection) are non-trivial and benefit from the EPG/Contract work's lessons.
5. **MCP tools** — add `create_epg`/`create_contract`/`create_l3out` to `mcp-server/tools/aci.py` (per `Platform-v2-Reference-Architecture.md` §7.8's already-named catalogue) once each object's generator/Terraform support lands, following the exact `create_tenant` pattern (Milestone 5): thin Pydantic schema, direct Nautobot write, no intent envelope.

## Phase B — Access and Fabric Policies

A materially different engineering problem from Phase A: these objects map to Nautobot's **Device/Interface/Cable/VLAN** data (DCIM/IPAM), not Tenant/VRF/Prefix, and the current ACI SSoT sync does not populate or use any of it today. Sequenced after Phase A so the generator/pipeline/MCP pattern is proven twice on the simpler problem (Tenant Policy) before tackling the harder data-modeling problem (Access Policy).

1. **Nautobot data model** — confirm what the existing ACI SSoT sync already populates for `Device`/`Interface` (leaf/spine nodes and their ports are synced today per `ACI-Nautobot-NaC-Architecture.md` Part 2's table — verify current depth against the live lab before assuming), and what's missing (VLAN pool ranges, physical/VMM domain assignments, AEP-to-domain bindings, interface policy groups). `VLANGroup` (IPAM) is the natural home for VLAN Pools per that document's mapping.
2. **Schema and Terraform** — reuse the YAML schema already designed in `ACI-Nautobot-NaC-Architecture.md` Part 6 (`apic.fabric_policies.vlan_pools[]`, `apic.access_policies.{physical_domains, vmm_domains, aaeps, leaf_interface_policy_groups, leaf_interface_profiles, leaf_profiles}[]`) as the starting draft — re-validate every field name against the current `netascode/aci` Terraform provider version before implementing, since that schema was drafted against an unspecified earlier provider version.
3. **Staging semantics** — Access Policies commonly need "define once, reference from many EPGs/interfaces" idempotency (e.g. an AEP reused across many leaf ports) more heavily than Tenant Policy does; validate the generator's determinism proof (Milestone 2's byte-identical-YAML check) still holds under this reuse pattern before considering the pipeline unchanged sufficient.
4. **MCP tools** — deferred until the generator/Terraform side is proven; Access Policy tools are not yet named in `Platform-v2-Reference-Architecture.md` §7.8 and should be scoped once the object model is settled. **Done 2026-09-01** -- see Phase B's Progress section below.

## What does not change

- The Execution Framework's 7 stages (Intent → Validation → Policy → Approval → Execution → Verification → Knowledge Capture) — unchanged, per ADR-017/018.
- GitLab CI's shared stage templates (`pipelines/includes/common.gitlab-ci.yml`) — unchanged; each new object type is generator/Terraform-module work, not new pipeline logic, per the reusable-pipeline design already proven across the ACI/future-EVPN/Fortinet/Azure domain split in `Platform-v2-Reference-Architecture.md` §6.
- NetAsCode YAML as the sole intent artifact — no parallel schema, per ADR-018.
- The MCP Server's domain-isolation rule (`tools/generic.py` never imports `tools/aci.py`) — unchanged, per §7.2.

---

# Progress

## Phase A item 1 — VRF and Bridge Domain attribute depth ✅ Complete (2026-07-29)

* Added 11 Nautobot Custom Fields: 3 on `ipam.vrf` (`aci_ip_data_plane_learning`, `aci_policy_control_enforcement_direction`, `aci_policy_control_enforcement_mode`) and 8 on `ipam.prefix` (`aci_bd_mac`, `aci_bd_arp_flooding`, `aci_bd_advertise_host_routes`, `aci_bd_l2_unknown_unicast`, `aci_bd_l3_unknown_multicast`, `aci_bd_multi_destination`, `aci_bd_ep_move_detect_mode`, `aci_bd_pim`) — BD-level fields live on `Prefix` (not a new BD model) since BD identity is already derived per-prefix in the current generator (see `transformer.py`'s module docstring); `igmp_policy` deferred (the provider models it as a relation to a separate `aci_igmp_snooping_policy` resource, not a scalar — out of proportion for this increment).
* Extended `platform/python/generator/client.py`'s GraphQL queries to fetch `_custom_field_data` for VRFs and Prefixes (confirmed exact field name via live GraphQL introspection, not assumed), and `transformer.py`'s `_build_vrfs`/`_build_bridge_domains` to emit these attributes **only when explicitly set** — an unset Custom Field means "let the netascode/aci Terraform provider's own ACI default apply," keeping generated YAML minimal (same convention `description` already uses).
* Extended `platform/terraform/aci/main.tf`'s `aci_vrf` and `aci_bridge_domain` resources to consume the new attributes via `lookup(..., null)`/`try(..., null)`. **Every attribute name and valid value was verified against the live `CiscoDevNet/aci` 2.20.0 provider** — via `terraform providers schema -json` for the full attribute list, and the provider's Terraform Registry documentation for exact valid value strings (e.g. `l2_unknown_unicast_flooding`: `"flood"`/`"proxy"`; `multi_destination_flooding`: `"bd-flood"`/`"drop"`/`"encap-flood"`) — not assumed from `ACI-Nautobot-NaC-Architecture.md`'s older, unverified schema draft, which used different field names in places (e.g. that document's `routing`/`host_routing` vs. the real provider's `unicast_routing`/`advertise_host_routes`). Confirmed the real provider has **no VRF-level `preferred_group` attribute** (the older doc's field) — Preferred Group Member is an EPG-level concept in this provider version, deferred to Phase A item 2.
* **Verified live:** set real Custom Field values on `web-tenant`'s VRF/BD, ran the generator against live Nautobot, confirmed the exact expected YAML was produced (`ip_data_plane_learning: disabled`, `policy_control_enforcement_direction: egress`, etc.), then reverted the test values (arbitrary, not deliberate business config) and confirmed the regenerated YAML matched the git-committed baseline exactly (no residual diff).
* `terraform validate` passes. A live `terraform plan` against the ACI simulator could not be completed this session — `172.30.46.103` was unreachable (confirmed via direct `curl` timeout), the same recurring external outage documented in Milestones 4-5's findings, not a defect in this change. Re-run `terraform plan`/`apply` once the simulator is reachable again for final live-apply confirmation.
* Added `tests/unit/test_transformer.py` (7 tests, no live infra required): baseline-output-unchanged regression guard, attribute emission when set, `None`-vs-`False` distinction (an explicit `false` must still be emitted, not treated as "unset"), and omission when unset. Full suite: 51/51 unit tests passing (44 pre-existing + 7 new).

## Phase A item 2 — Application Profiles and EPGs ✅ Complete (2026-07-29)

* Confirmed the real `CiscoDevNet/aci` 2.20.0 resource names via `terraform providers schema -json` (the older `ACI-Nautobot-NaC-Architecture.md` draft's assumed `aci_epg` resource name does not exist — the real resource is `aci_application_epg`) and the Terraform Registry docs (`preferred_group_member` valid values `"exclude"`/`"include"`, default `"exclude"`; `relation_to_bridge_domain` is a nested attribute with sub-field `bridge_domain_name`, not a block).
* Modeled EPGs as Nautobot **VLAN** objects (`ipam.vlan`) rather than adding a new Nautobot plugin/custom model — ADR-020 originally proposed a lightweight custom model, but VLAN already has the right shape (name + an integer identifier) and Custom Fields close the gap cleanly, consistent with item 1's Custom-Field-first approach. Added 3 Custom Fields on `ipam.vlan`: `aci_application_profile` (text, the AP name this EPG belongs to), `aci_epg_bridge_domain` (text, the BD name this EPG binds to), `aci_epg_preferred_group_member` (boolean).
* **Strictly opt-in, unlike item 1's read-if-exported-object-already-exists pattern:** a VLAN is only exported as an EPG when *both* `aci_application_profile` and `aci_epg_bridge_domain` are explicitly set — Nautobot's VLAN model is used for plenty of non-ACI IPAM purposes, so an ordinary VLAN with neither field set must never be silently turned into ACI configuration.
* Extended `client.py` (`_QUERY_VLANS` + `get_vlans()`), `transformer.py` (`_build_application_profiles()`, new `vlans` parameter on `build_netascode_yaml()`, defaults to `None`/empty for backward compatibility), `generate_aci.py` (fetches and passes `vlans`), and `main.tf` (`local.application_profiles`/`local.endpoint_groups` flat maps + `aci_application_profile`/`aci_application_epg` resources, following the exact same `for_each`-flat-map and `lookup(...)`/`try(...)` null-handling pattern as items 1's VRF/BD attributes).
* Added 6 new unit tests to `tests/unit/test_transformer.py` (baseline unchanged without `vlans`, VLAN skipped when either custom field missing, AP/EPG emitted when both set, EPGs correctly grouped by AP name across multiple VLANs, `preferred_group_member` only emitted when `True`). Full suite: 56/56 passing.
* **Verified live, including a real `apply`+`destroy` (not just plan)** — since AP/EPG are genuinely new resource types never before created by this module, a plan-only proof (sufficient for item 1's attribute-only change) was not considered enough:
  1. Created a real test VLAN in Nautobot (`web-epg`, vid 100, under `web-tenant`, `aci_application_profile=web-ap`, `aci_epg_bridge_domain=web-bd`).
  2. Regenerated `tenants.yaml` from live Nautobot — generator correctly emitted `application_profiles: [{name: web-ap, endpoint_groups: [{name: web-epg, bridge_domain: web-bd, description: ...}]}]` for `web-tenant`.
  3. `terraform plan` showed exactly the two new resources (`aci_application_profile.this["web-tenant/web-ap"]`, `aci_application_epg.this["web-tenant/web-ap/web-epg"]` — both `will be created`, correct `parent_dn`s, `relation_to_bridge_domain.bridge_domain_name = "web-bd"`), plus 4 unrelated pre-existing `materialization-test-*` debris adds already known from earlier sessions.
  4. Ran a **targeted `terraform apply`** for just those two resources — both created successfully in the real APIC (`Apply complete! Resources: 2 added`, confirmed via `terraform state list` and a follow-up no-target `terraform plan` on the same two resources showing "No changes").
  5. Ran a **targeted `terraform destroy`** for the same two resources (`Destroy complete! Resources: 2 destroyed`), then deleted the test VLAN from Nautobot via the API, then regenerated `tenants.yaml` again and confirmed it reports `vlans=0`, matching the pre-test baseline with no `application_profiles` key for `web-tenant`.
* GOTCHA (tooling, not code): `generate_aci.py --dry-run` interleaves its own `[generator] ...` progress lines with the actual YAML on **stdout** (not stderr as initially assumed) — parsing dry-run output as pure YAML requires splitting on the `--dry-run: output below` marker line first, or (simpler) writing straight to `--output <dir>` and inspecting the file instead of relying on `--dry-run`'s console output.

## Phase A item 3 — Contracts / Filters / Subjects ✅ Complete (2026-07-29)

* Confirmed real `CiscoDevNet/aci` 2.20.0 resource names via `terraform providers schema -json` and Registry docs: `aci_contract`, `aci_filter`, `aci_filter_entry`, `aci_contract_subject`, `aci_epg_to_contract`. Deliberately **not** used: `aci_contract_subject_filter` (a separate resource for binding filters to subjects) — `aci_contract_subject` already exposes `relation_vz_rs_subj_filt_att` (a plain Set-of-Filter-DN attribute) which is sufficient for this MVP scope (no per-filter `action`/`directive`/`priority_override` requirement yet); also **not** used: the legacy inline `filter`/`filter_entry` blocks nested directly under `aci_contract` (works, but couples filter lifecycle to the contract and prevents filter reuse across contracts — the separate-resource approach matches this module's existing "each ACI object = its own resource, referenced by DN" convention).
* **New Nautobot data modeling problem, genuinely different from items 1-2:** Contracts/Filters/Subjects have no natural 1:1 home in Nautobot's Tenant/VRF/Prefix/VLAN model the way VRF-attributes (item 1) or EPGs-as-VLANs (item 2) did. Per ADR-020's own contingency ("representing them as structured Custom Field data on the Tenant until real usage proves whether first-class Nautobot objects are worth the modeling cost"), used Nautobot's **JSON-typed Custom Field** (confirmed available via the Custom Field API's `type` choices) rather than new Nautobot models:
  - `aci_contracts` (JSON) on `tenancy.tenant`: `{"filters": [{"name", "entries": [{"name", "ether_type", "ip_protocol", "dest_from_port", "dest_to_port"}]}], "contracts": [{"name", "scope", "subjects": [{"name", "filters": [...]}]}]}`.
  - `aci_epg_contracts` (JSON) on `ipam.vlan` (the same VLAN objects item 2 already uses for EPGs): `{"provided": [contract_name, ...], "consumed": [contract_name, ...]}`.
  - Same opt-in philosophy as items 1-2: an empty/missing JSON field means no filters/contracts/contract-references are emitted for that tenant/EPG.
* Extended `client.py` (added `_custom_field_data` to the top-level `tenants` GraphQL query — it was previously only fetched on nested `vrfs`), `transformer.py` (`_build_contracts_and_filters()`, and `_build_application_profiles()` extended to read `provided_contracts`/`consumed_contracts` from each EPG's VLAN), and `main.tf` (`local.filters`/`local.filter_entries`/`local.contracts`/`local.contract_subjects`/`local.epg_contract_relations` flat maps + `aci_filter`/`aci_filter_entry`/`aci_contract`/`aci_contract_subject`/`aci_epg_to_contract` resources — same `for_each`-flat-map convention as items 1-2). `epg_contract_relations` is built by iterating each EPG's `provided_contracts`/`consumed_contracts` lists and expanding to one `aci_epg_to_contract` resource per (EPG, contract, direction) tuple, with `contract_type` set to `"provider"`/`"consumer"` accordingly.
* Added 3 new unit tests to `tests/unit/test_transformer.py` (baseline unchanged when `aci_contracts` unset, filters/contracts correctly emitted from the JSON custom field, EPG `provided_contracts`/`consumed_contracts` emitted independently and only when set). Full suite: 59/59 passing.
* **Verified live with a real `apply`+`destroy`** (same standard as item 2, since these are all genuinely new resource types): set `aci_contracts` on `web-tenant` (one filter `web-filter` with an HTTP entry, one contract `web-to-db` with a subject referencing that filter) and created a test EPG VLAN with `aci_epg_contracts: {"provided": ["web-to-db"]}`.
  1. Regenerated `tenants.yaml` — generator correctly emitted `filters`, `contracts`, and the EPG's `provided_contracts: ["web-to-db"]`, exactly matching the JSON custom field's structure.
  2. `terraform plan` (targeted at the 5 new resource addresses) showed **`Plan: 7 to add`** — the 5 new resources plus the Application Profile + EPG they depend on (destroyed at the end of item 2's own cleanup, so correctly proposed again here).
  3. `terraform apply` (same targets) — **`Apply complete! Resources: 7 added`** — all created successfully in the real APIC, confirmed via `terraform state show` on the Contract Subject: `relation_vz_rs_subj_filt_att = ["uni/tn-web-tenant/flt-web-filter"]`, proving the filter-DN reference resolved correctly.
  4. A follow-up targeted `terraform plan` showed **"No changes. Your infrastructure matches the configuration."**
  5. `terraform destroy` (same 7 targets) — **`Destroy complete! Resources: 7 destroyed`** — then deleted the test VLAN from Nautobot, cleared `web-tenant`'s `aci_contracts` custom field back to `null`, regenerated `tenants.yaml`, and confirmed it reports `vlans=0` with no `application_profiles`/`filters`/`contracts` keys for `web-tenant`, matching the pre-test baseline.

## Phase A item 4 — L3Out ✅ Complete, logical-only MVP scope (2026-07-29)

* **Scope-narrowing discovery before implementation:** checked Nautobot's DCIM directly (`GET /api/dcim/devices/`) and found **zero leaf/spine devices synced — only the APIC controller itself (`apic1`)**. A real L3Out's physical interface attachment (Logical Node Profile + Logical Interface Profile + routed interface path, e.g. node 101 `eth1/1`) fundamentally requires real fabric node/interface inventory, which does not exist in this Nautobot instance today (that's Phase B's problem — VLAN pools, domains, AEPs, interface policy groups all map to the same missing DCIM data). This is a bigger gap than ADR-020's original item 4 note anticipated ("L3Out's Nautobot modeling questions... are non-trivial") — it isn't just a modeling question, the underlying data doesn't exist at all yet.
* **User decision (asked directly given the scope impact):** proceed with a **logical-only L3Out MVP** — the L3Out object, its VRF association, External EPGs, and their subnets/contract references, explicitly excluding physical fabric attachment, OSPF/BGP protocol config, and Logical Node/Interface Profiles. This reserves the L3Out and lets Contracts bind to it, but does **not** by itself pass real external traffic in a real APIC without additional manual interface/routing configuration — documented as a known, deliberate limitation, not an oversight.
* Confirmed real `CiscoDevNet/aci` 2.20.0 resource names via `terraform providers schema -json` and Registry docs: `aci_l3_outside` (the L3Out object; required `relation_l3ext_rs_ectx` VRF relation), `aci_external_network_instance_profile` (the External EPG — chosen over the newer `aci_external_epg` nested-relation-block resource specifically because `aci_l3_ext_subnet`'s own official example only documents attaching via `external_network_instance_profile_dn`, and its direct `relation_fv_rs_prov`/`relation_fv_rs_cons` Set-of-DN attributes match this module's existing simple-relation convention from item 3's Contract Subject), `aci_l3_ext_subnet` (the External EPG subnet, e.g. `0.0.0.0/0`).
* Same Custom-Field-JSON approach as item 3: new `aci_l3outs` (JSON) on `tenancy.tenant`: `{"l3outs": [{"name", "vrf", "description", "external_epgs": [{"name", "provided_contracts": [...], "consumed_contracts": [...], "subnets": [{"ip", "scope": [...], "aggregate"}]}]}]}`. No `client.py` changes needed (tenant `_custom_field_data` already fetched since item 3).
* Extended `transformer.py` (`_build_l3outs()`, wired into `build_netascode_yaml()`) and `main.tf` (`local.l3outs`/`local.external_epgs`/`local.external_epg_subnets` flat maps + `aci_l3_outside`/`aci_external_network_instance_profile`/`aci_l3_ext_subnet` resources, same `for_each`-flat-map convention as items 1-3).
* Added 2 new unit tests (baseline unchanged when `aci_l3outs` unset, L3Out/External EPG/Subnet correctly emitted from the JSON custom field). Full suite: 61/61 passing.
* **Verified live with a real `apply`+`destroy`:** set `aci_l3outs` on `web-tenant` (one L3Out `l3out-internet` on `web-vrf`, one External EPG `ext-epg-internet` with a `0.0.0.0/0` subnet).
  1. Regenerated `tenants.yaml` — generator correctly emitted the `l3outs` structure exactly matching the JSON custom field.
  2. `terraform plan` (targeted at the 3 new resource addresses) showed **`Plan: 3 to add`**, with `aci_l3_outside`'s `relation_l3ext_rs_ectx` correctly resolving to `uni/tn-web-tenant/ctx-web-vrf`.
  3. `terraform apply` (same targets) — **`Apply complete! Resources: 3 added`** — all created successfully in the real APIC.
  4. A follow-up targeted `terraform plan` showed **"No changes. Your infrastructure matches the configuration."**
  5. `terraform destroy` (same 3 targets) — **`Destroy complete! Resources: 3 destroyed`** — then cleared `web-tenant`'s `aci_l3outs` custom field back to `null`, regenerated `tenants.yaml`, and confirmed no `l3outs` key remained for `web-tenant`, matching the pre-test baseline.

## Phase A — ✅ All items complete (2026-07-29)

Items 1-4 are all implemented, unit-tested, and live-verified against the real ACI simulator. Item 4's logical-only L3Out scope is the one deliberate, documented gap — full physical L3Out attachment (routed interfaces, OSPF/BGP) is blocked on Phase B's Device/Interface data-modeling work (see Phase B section above), not on anything specific to Tenant Policy Depth.

## Phase B — ✅ Complete, logical-only scope (2026-07-29)

* **Device-sync investigation (before any generator/Terraform work):** attempted to fix `nautobot_ssot`'s ACI Data Source job so it could sync real leaf/spine `Device`/`Interface` inventory into Nautobot, per this ADR's original Phase B item 1 plan. Root-caused 3 distinct real bugs live (not guessed): (1) `NautobotAdapter.load_tenants()` filters `Tenant.objects.filter(tags=self.site_tag)` — any Nautobot object missing the "ACI-Lab" tag is invisible to the sync's diff matching, causing bogus re-create attempts that collide with DB unique constraints; found and deleted 28 debris tenants (`materialization-test-*`/`real-tf-test-*`/`real-tf-fail-*`, plus their dependent VRFs/Prefixes/Namespaces) left over from earlier sessions' integration tests, and tagged the 2 legitimate ones (`web-tenant`, `new-app-tenant`); (2) Nautobot's `Tag.content_types` is an allow-list enforced by the API on write but not by whatever path originally created the tag — had to PATCH the "ACI-Lab" tag's own `content_types` to include `tenancy.tenant`/`dcim.device`/`ipam.vrf`/`ipam.prefix`/`ipam.vlan` before it could be applied to new objects; (3) `nautobot_ssot`'s `NautobotTenant.create()` does an unguarded `Namespace.objects.create()` with no wrapping transaction across the whole sync — a partially-failed prior run left one tenant with no matching Namespace, diagnosed by diffing `Tenant`/`Namespace` name sets. After fixing all three, hit a 4th, different bug (`VRF.MultipleObjectsReturned` in the `common`/`infra`/`mgmt` system tenants' VRF handling) — three layers deep into Tenant/VRF plumbing Phase B doesn't actually need, asked the user whether to keep debugging or pivot; **user chose to pivot to manually bootstrapping just the 2 real leaf Devices**.
* **Bigger discovery:** the ACI simulator genuinely has 2 real leaf nodes (queried `GET /api/class/fabricNode.json` directly: node 101 "leaf-a", node 102 "leaf-b", `role=leaf`) — contradicting Phase A item 4's assumption that "zero leaf/spine exist" was purely about Nautobot's state; the fabric itself has leaf switches. But both leaf `fabricNode` records have a **blank `model` attribute**, the simulator's `eqptCH` (equipment chassis) class is completely unimplemented ("unresolved class for eqptCH"), and the simulator has **zero `l1PhysIf` objects anywhere** — confirmed via both a node-scoped query (failed: "node marked unavailable", meaning this simulator does not proxy queries down to individual switch MITs at all) and a class-wide query (`totalCount: 0`). **This is a hard, permanent simulator limitation, not a code/sync bug**: no physical interface data can ever be obtained from this environment, by any method (SSoT job or manual). Confirmed the user's own suspicion that this is a Cisco-simulator-specific constraint, not a generator/Terraform gap.
* Manually bootstrapped the 2 leaf Devices in Nautobot (bypassing the buggy bulk resync, backed by real fabricNode node IDs): a placeholder `ACI-LEAF-SIM` DeviceType (the existing single Manufacturer object has a blank `name` too — a separate pre-existing lab data-quality quirk, noted but out of scope), a new `leaf` Role, and Devices `leaf-a` (`aci_node_id=101`)/`leaf-b` (`aci_node_id=102`), matching `apic1`'s existing conventions.
* **Scope decision (confirmed with the user):** proceed with Phase B as **logical-only** — model the fabric-wide policy *objects* (VLAN Pools, Physical Domains, AEPs, Leaf Interface Policy Groups) and their relations to each other, but never attempt physical port/interface binding (Leaf/Interface Profiles + Access Port Selectors), since that's permanently blocked on this simulator regardless of approach.
* Confirmed real `CiscoDevNet/aci` 2.20.0 resource names via `terraform providers schema -json` and Registry docs: `aci_vlan_pool` + `aci_ranges` (VLAN Pool encap blocks), `aci_physical_domain`, `aci_attachable_access_entity_profile` (AEP), `aci_leaf_access_port_policy_group` (the interface policy group itself — creatable with zero physical port data, since it only groups per-port policies + an AEP relation). Used the AEP's non-deprecated `relation_to_domains` (list-of-object, `{target_dn}`) instead of the older `relation_infra_rs_dom_p` (plain Set-of-DN) after `terraform plan` surfaced a live deprecation warning for the latter — a first for this ADR's implementation history, confirms the value of always plan-checking for provider warnings, not just errors.
* **New Nautobot data-modeling problem, same class as items 3-4:** these objects are fabric-wide, not Tenant-scoped, so — same Custom-Field-JSON approach — added `aci_fabric_policies` (JSON) on `dcim.location` (the Location represents the ACI fabric/site): `{"vlan_pools": [{"name", "alloc_mode", "ranges": [{"from", "to", "alloc_mode", "role"}]}], "physical_domains": [{"name", "vlan_pool"}], "aeps": [{"name", "domains": [...]}], "leaf_interface_policy_groups": [{"name", "aep"}]}`. Aggregated across all Locations with the field set (multi-site safe, though this lab only has one).
* Extended `client.py` (`_QUERY_LOCATIONS` + `get_locations()`), `transformer.py` (`_build_fabric_and_access_policies()`, new `locations` param on `build_netascode_yaml()`, output now has top-level `apic.fabric_policies`/`apic.access_policies` keys sibling to `apic.tenants`, not nested per-tenant), `generate_aci.py` (fetches and passes `locations`), and `main.tf` (`local.vlan_pools`/`vlan_pool_ranges`/`physical_domains`/`aeps`/`leaf_interface_policy_groups` + 5 new resources, same `for_each`-flat-map convention as Phase A).
* Added 3 new unit tests (baseline unchanged when no location has the field set, fabric/access policies correctly emitted, aggregation across multiple locations). Full suite: 64/64 passing.
* **Verified live with a real `apply`+`destroy`:** set `aci_fabric_policies` on the ACI-Lab Location (one VLAN Pool `phase-b-test-pool` with a `vlan-100`-`vlan-200` range, one Physical Domain bound to it, one AEP bound to the domain, one Leaf Interface Policy Group bound to the AEP).
  1. Regenerated `tenants.yaml` — generator correctly emitted `fabric_policies`/`access_policies` exactly matching the JSON custom field.
  2. `terraform plan` (targeted at the 5 new resource addresses) showed **`Plan: 5 to add`**, with a live deprecation warning for `relation_infra_rs_dom_p` — fixed by switching to `relation_to_domains`, then re-planned clean.
  3. `terraform apply` (same targets) — **`Apply complete! Resources: 5 added`** — all created successfully in the real APIC.
  4. A follow-up targeted `terraform plan` showed **"No changes. Your infrastructure matches the configuration."**, and `terraform state show` confirmed the AEP's `relation_to_domains` resolved to the real Physical Domain DN (`uni/phys-phase-b-test-physdom`).
  5. `terraform destroy` (same 5 targets) — **`Destroy complete! Resources: 5 destroyed`** — then cleared the Location's `aci_fabric_policies` custom field back to `null`, regenerated `tenants.yaml`, and confirmed no `fabric_policies`/`access_policies` keys remained, matching the pre-test baseline.
* **MCP tools added (2026-09-01):** `create_vlan_pool`, `create_physical_domain`, `create_aep`, `create_leaf_interface_policy_group` in `mcp-server/src/mcp_server/tools/aci.py`, following the exact same pattern as Phase A's tools (thin Pydantic schema in `schemas/aci.py`, direct Nautobot write via `clients/nautobot.py`, read-merge-write against the `aci_fabric_policies` JSON Custom Field on the Location object -- same convention as `create_contract`/`create_l3out`'s Tenant-scoped JSON Custom Field writes). `create_vlan_pool` appends one encap range, creating the pool first if it doesn't already exist; `create_aep` merges `domains` into any already bound rather than replacing them. Unit-tested (8 new tests: schema validation + tool-dispatch against a fake Nautobot client, following `test_tools_aci.py`'s established convention) -- full suite 65/65 passing. **A real regression was caught by the existing `test_registry.py::test_real_tools_have_schema_derivable_signature` test**: the first draft of all four new request schemas put `location` (a defaulted field) before required fields like `name`, which `inspect.Signature` rejects ("non-default argument follows default argument") -- this is the exact class of bug that test was written to catch after the real Milestone 5 incident, and it worked as intended. Fixed by reordering every new schema's fields (required first, defaults last). **Live-verified over the real MCP protocol (2026-09-04)** -- see this ADR's own MCP-protocol verification note in the Phase D section below, which covers all six still-outstanding tools (this phase's four plus Phase D's two) in one pass.

## Phase C — ✅ Complete: Fabric-wide POD Policies (NTP/DNS/SNMP) (2026-07-30)

Revisited the "explicitly out of scope" call below after a concrete need arose (user request: "do fabric-wide config support").

* **Provider research (real, not assumed):** re-checked `terraform providers schema -json` for `CiscoDevNet/aci` 2.20.0 — confirmed no dedicated typed resource exists for NTP Policy or DNS Profile. Only `aci_snmp_community`/`aci_snmp_user` exist as typed children for SNMP (attached via `parent_dn`). NTP/DNS must go through the generic `aci_rest_managed` resource (attributes: `dn`, `class_name`, `content` (map), `content_on_destroy` (map), `child` blocks with `class_name`/`content`/`rn`).
* **Live APIC attribute discovery:** confirmed by directly querying and test-creating/deleting real objects against the simulator — `datetimePol` at `uni/fabric/time-default` (`name`, `adminSt`, `authSt`, `serverState`, `masterMode`, `StratumValue`), `datetimeNtpProv` child (`name` [server address], `preferred`, `minPoll`, `maxPoll`), `dnsProfile` at `uni/fabric/dnsp-default` (`name`, `IPVerPreference`), `dnsProv`/`dnsDomain` children (`addr`/`preferred`, `name`/`isDefault`), `snmpPol` at `uni/fabric/snmppol-default` (`name`, `adminSt`, `contact`, `loc`). All three parent DNs are **mandatory ACI system singletons that exist by default in every fabric** — confirmed baseline before any change: `datetimePol` (`adminSt=enabled`, `StratumValue=8`, `masterMode=disabled`, `serverState=disabled`), `dnsProfile` (`IPVerPreference=IPv4`), `snmpPol` (`adminSt=disabled`, `contact=""`, `loc=""`).
* **Design:** reused the same fabric-wide Custom-Field-JSON pattern as Phase B — extended the existing `aci_fabric_policies` JSON field on `dcim.location` with `ntp`/`dns`/`snmp` keys (singleton semantics: last Location with the field set wins, unlike Phase B's list-shaped keys which merge across Locations). Extended `transformer.py`'s `_build_fabric_and_access_policies()` to read the 3 new keys and emit them under the existing `apic.fabric_policies.{ntp,dns,snmp}` YAML keys. Extended `main.tf` with `aci_rest_managed.ntp_policy`/`dns_profile`/`snmp_policy` (dynamic `child` blocks for servers/domains) plus `aci_snmp_community.this` (typed resource, `parent_dn` + `depends_on`).
* **Verified live with a real `apply`+`destroy` — and caught a real bug in the process:**
  1. Regenerated `tenants.yaml` with test `ntp`/`dns`/`snmp` data on the ACI-Lab Location — generator correctly emitted `apic.fabric_policies.{ntp,dns,snmp}`.
  2. `terraform validate` / targeted `terraform plan` — clean 4-resource plan.
  3. Targeted `terraform apply` — **`Apply complete! Resources: 4 added`** — confirmed live via direct APIC queries (NTP provider child, DNS server/domain children, SNMP community all present with correct attributes).
  4. Targeted `terraform destroy` — Terraform reported **`Destroy complete! Resources: 4 destroyed`**, but a follow-up live APIC query found **`totalCount: 0`** for all three DNs: `aci_rest_managed`'s default destroy behavior deletes the entire target DN, not just the attributes/children this resource added. Because these three DNs are mandatory ACI system singletons, this **deleted real fabric-wide objects from the shared lab simulator**, not just this resource's managed state.
  5. **Manually recovered** by POSTing the original pre-test attribute values back to all three DNs, then confirmed via read-back that all three objects were restored exactly (only `modTs` differs).
  6. **Fixed the root cause:** added `content_on_destroy` to all three `aci_rest_managed` resources, set to the confirmed original baseline content (`{adminSt = "enabled"}` for NTP, `{IPVerPreference = "IPv4"}` for DNS, `{adminSt = "disabled", contact = "", loc = ""}` for SNMP) — this tells the provider what content to revert the DN to on destroy, instead of deleting the DN.
  7. **Re-verified the fix live:** re-applied the same 4 resources, then re-ran targeted `terraform destroy` — this time all three singleton objects survived (`totalCount: 1` for all three, matching the original baseline; only the `annotation` field now permanently shows `orchestrator:terraform`, a harmless cosmetic side effect of `aci_rest_managed` ever having managed the object). Terraform state confirmed clean (no leftover resource records) after the final destroy.
  8. Cleared the ACI-Lab Location's `aci_fabric_policies` custom field back to `null`, matching the pre-test baseline.
* **Generalizable lesson:** any generic "manage any object" Terraform resource (`aci_rest_managed` and equivalents in other providers) must always set destroy-time content explicitly when targeting objects that are mandatory singletons in the target system — the default destroy behavior of deleting the DN is unsafe for objects that cannot actually be removed from the real system.

## ADR-020 — All phases complete (2026-07-30)

Phase A (items 1-4), Phase B, and Phase C are all implemented, unit-tested, and live-verified against the real ACI simulator. Two deliberate, documented scope limitations remain, both stemming from the same root cause (this simulator has zero real leaf/spine interface data, confirmed directly via APIC API queries): L3Out has no physical interface/OSPF/BGP attachment (item 4), and Access Policies have no physical port/interface binding (Phase B). Both are permanent constraints of this specific simulator, not gaps in the generator/Terraform pattern — extending either would require a heavier simulator or real hardware. Fabric Policies (Phase C) are complete with no such limitation — NTP/DNS/SNMP are logical, POD-wide configuration with no physical interface dependency.

## Phase D — VMM Domain Integration ✅ Complete (2026-09-04)

Revisited "VMM Domain integration — Not started" from `Platform-Status-and-Pending-Items.md` after the user asked to prioritize it: unlike the two simulator-blocked items above, this lab has a real vCenter (`netvcenter.sc.gr`, datacenter "NetworkLab Datacenter") available, so VMM Domain integration can be live-verified immediately with no hardware blocker — VMM Domain objects require no physical leaf/spine port data at all.

* **Provider research (real, not assumed):** confirmed exact resource/attribute names via both the Terraform Registry docs and `terraform providers schema -json` against the installed `CiscoDevNet/aci` 2.20.0 provider (the same version used throughout this ADR) — the live schema caught a real discrepancy with the docs: `aci_vmm_domain`'s `parent_dn` is documented under "Required" but the installed provider schema shows it as **optional** (it has a provider-side default). `aci_vmm_controller` (`vmm_domain_dn`/`name`/`host_or_ip`/`root_cont_name` required; `dvs_version`/`relation_vmm_rs_acc` optional) and `aci_vmm_credential` (`name` required; `parent_dn`/`username`/`password` optional) matched the docs exactly.
* **Design decision — credentials never touch Nautobot or the generated YAML:** the vCenter Controller's actual username/password are supplied at `terraform apply` time via new sensitive Terraform variables (`vmm_vcenter_username`/`vmm_vcenter_password`, `variables.tf`), exactly matching how the APIC's own `aci_username`/`aci_password` are handled — never persisted in a Nautobot Custom Field or committed YAML. Only the ACI Credential object's own *name* (not a secret) is stored in Nautobot.
* **Nautobot data model:** same Custom-Field-JSON pattern as Phase B/C — extended the existing `aci_fabric_policies` JSON field on `dcim.location` with a new `vmm_domains` list key: `{"name", "vendor" (default "VMware"), "vlan_pool" (optional), "controller": {"name", "host_or_ip", "root_cont_name", "dvs_version"}, "credential": {"name"}}`. Aggregated across all Locations with the field set, same as Phase B/C's list-shaped keys.
* Extended `transformer.py`'s `_build_fabric_and_access_policies()` to collect `vmm_domains` (list-shaped, same aggregation-across-Locations convention as `vlan_pools`), `main.tf` with `local.vmm_domains`/`local.vmm_controllers` flat maps + `aci_vmm_domain`/`aci_vmm_credential`/`aci_vmm_controller` resources (`aci_vmm_credential.this` only created for domains that actually specify a `credential` entry; `aci_vmm_controller.this.relation_vmm_rs_acc` only set when a matching credential exists, avoiding a broken reference for a controller-only domain), and `variables.tf` with the two new sensitive variables.
* Added 6 new unit tests to `tests/unit/test_transformer.py` (emission when set, absence when unset, aggregation across multiple Locations) — full generator suite 31/31 passing. Added `CreateVmmDomainRequest` to `mcp-server/schemas/aci.py` (required fields first, per the established `inspect.Signature`-derivability convention this ADR's Phase B tools already learned the hard way) and `create_vmm_domain` to `mcp-server/tools/aci.py` + `NautobotClient.create_vmm_domain()`, following the exact same read-merge-write-against-Location pattern as `create_vlan_pool`. Added 6 new mcp-server unit tests (2 schema, 1 tool-dispatch, plus the existing signature-derivability guard) — full mcp-server suite 71/71 passing.
* **Verified live with a real `apply`+`destroy` against both real vCenter and the real ACI simulator (not just plan):**
  1. Queried vCenter's REST API directly (`/api/vcenter/datacenter`) to confirm the real Datacenter name ("NetworkLab Datacenter") rather than guessing it.
  2. Set a test `vmm_domains` entry on the ACI-Lab Location's `aci_fabric_policies` Custom Field via the Nautobot API, regenerated `tenants.yaml` from live Nautobot — generator correctly emitted `apic.fabric_policies.vmm_domains` matching the Custom Field exactly.
  3. `terraform plan` (targeted at the 3 new resource addresses, `TF_VAR_vmm_vcenter_username`/`TF_VAR_vmm_vcenter_password` set to the real vCenter admin credential) showed **`Plan: 3 to add, 0 to change, 0 to destroy`**.
  4. `terraform apply` (same targets) — **`Apply complete! Resources: 3 added`**.
  5. Independently confirmed via a direct, separately-authenticated APIC API query (`GET .../dom-phase-d-test-vmm.json?query-target=subtree`) — real `vmmDomP`/`vmmCtrlrP`/`vmmUsrAccP`/`vmmRsAcc` objects present with the correct `hostOrIp` (`netvcenter.sc.gr`), `rootContName` (`NetworkLab Datacenter`), and `usr` (`administrator@netvsphere.local`) attributes, and the Controller-to-Credential relation (`vmmRsAcc`) correctly resolved.
  6. `terraform destroy` (same 3 targets) — **`Destroy complete! Resources: 3 destroyed`** — re-confirmed via an independent APIC query showing `totalCount: 0` for the destroyed domain's DN.
  7. Cleared the ACI-Lab Location's `aci_fabric_policies` Custom Field back to `null`, matching the pre-test baseline.
* **No simulator/hardware limitation, unlike Phase B/Phase A item 4** — VMM Domain, Controller, and Credential are all logical ACI-side objects; they don't require any physical leaf/spine port data to create. The only thing this MVP doesn't cover is binding an EPG to the VMM Domain (`relation_vmm_rs_mgmt_e_pg`-style EPG↔domain association) — deferred, same as Phase A item 2's original `static_ports`/domain-binding scope, since it needs its own short design note on how an EPG selects which of possibly-several VMM Domains it deploys to.

### Follow-on design note — EPG-to-Domain Binding (implemented, 2026-09-04)

Scoped ahead of implementation, per this ADR's own convention (see "Negative/risk" above) — confirmed against the real installed provider schema, not assumed:

* **Real resource/attribute (confirmed via `terraform providers schema -json`):** `aci_application_epg.relation_to_domains` is a single, generic relation (Terraform Set of nested objects) that binds an EPG to **any** domain kind — Physical, VMM, L2 External, or L3 External — via one attribute, not a separate relation per domain type. Each set member's key field is `target_dn` (required in practice, though the schema itself marks the whole attribute optional+computed); the VMM-specific fields that actually matter for a real deployment are `resolution_immediacy` (`"immediate"`/`"lazy"`/`"pre-provision"`) and `deployment_immediacy` (`"immediate"`/`"lazy"`) — both now supported as optional pass-through fields.
* **Nautobot data model:** EPGs are already modeled as `ipam.vlan` objects with two scalar Custom Fields (`aci_application_profile`, `aci_epg_bridge_domain`) and one JSON Custom Field (`aci_epg_contracts`, added in Phase A item 3). Added a third JSON Custom Field, `aci_epg_domains`, on the same VLAN object: `{"domains": [{"name": "<domain_name>", "domain_type": "physical"|"vmm", "resolution_immediacy": "...", "deployment_immediacy": "..."}]}`. `domain_type` is required (not inferred) because a Physical Domain and a VMM Domain could share the same `name` — Terraform's `relation_to_domains.target_dn` needs the *correct* domain's DN, so the generator/Terraform resolve against the correct `aci_physical_domain`/`aci_vmm_domain` resource map explicitly rather than guessing by name collision.
* **Generator:** `_build_application_profiles()` in `transformer.py` reads `aci_epg_domains` off the EPG's VLAN Custom Field and emits a `domains` list on the EPG dict, same `if x := y.get(...)` opt-in pattern as `provided_contracts`/`consumed_contracts`.
* **Terraform:** `aci_application_epg.this` now sets `relation_to_domains` (null when the EPG has no `domains` entries) — each entry's `target_dn` resolved via `aci_physical_domain.this[name].id` or `aci_vmm_domain.this[name].id` depending on `domain_type`, with `resolution_immediacy`/`deployment_immediacy` passed through via `lookup(..., null) != null ? {...} : {}` merges (left unmanaged/ACI-default when omitted).
* **MCP tool:** added `bind_epg_domain` (schema `BindEpgDomainRequest`) — binds an *already-existing* EPG to a Physical or VMM Domain by read-merge-write against the EPG's own `aci_epg_domains` Custom Field (`NautobotClient.bind_epg_domain()`), matching an existing (domain, domain_type) pair updates its immediacy settings in place instead of duplicating the entry (same convention as `create_aep`'s domains list). Deliberately a separate tool from `create_epg`, since domain binding is a common post-creation operation, not only a creation-time input.
* **Unit-tested:** generator test `test_epg_domains_emitted_only_when_set` (32/32 generator suite passing); mcp-server schema tests (`test_valid_bind_epg_domain_request_defaults`, `test_bind_epg_domain_request_with_immediacy`, `test_invalid_bind_epg_domain_type_rejected` ×4 params) and tool-dispatch test `test_bind_epg_domain_passes_fields_through` (78/78 mcp-server suite passing).
* **Verified live with a real `apply`+`destroy` against the real ACI simulator:**
  1. Discovered a real gap while live-verifying: `aci_epg_domains` had never been defined as an actual Nautobot Custom Field (unlike `aci_epg_contracts`, defined during Phase A item 3) — Nautobot silently drops unknown `custom_fields` keys on write with no error, so the first `bind_epg_domain()` call appeared to succeed but didn't persist. Fixed by creating the Custom Field definition via `POST /api/extras/custom-fields/` (`content_types: ["ipam.vlan"]`, `type: json`, `key: aci_epg_domains`), then re-ran the bind and confirmed persistence via a follow-up GET.
  2. Built a throwaway test tenant (`domtest`) end-to-end via the actual MCP client methods (`create_tenant`/`create_vrf`/`create_bridge_domain`/`create_epg`/`create_physical_domain`/`bind_epg_domain`) against live Nautobot, then regenerated `tenants.yaml` — generator correctly emitted the EPG's `domains` list.
  3. The full generator output couldn't be planned as-is: it hits a pre-existing, unrelated `local.bridge_domains` "Duplicate object key" error (tenant `ACI:Sales` has two Bridge Domains both named `DB_BD` at different prefixes — data debris from earlier testing, left unfixed as out-of-scope). Worked around by extracting a scoped YAML containing only the `domtest` tenant + its physical domain, and running `plan`/`apply`/`destroy` in an **isolated Terraform working directory with a fresh empty local state** (copied `*.tf` to `/tmp`, `terraform init` there) rather than against the shared `platform/terraform/aci/` state, which already carries many unrelated prior test tenants and would otherwise have planned their destruction.
  4. `terraform plan` (isolated state) — **`Plan: 7 to add, 0 to change, 0 to destroy`**.
  5. `terraform apply` — **`Apply complete! Resources: 7 added`**.
  6. Independently confirmed via a direct, separately-authenticated APIC API query (`GET .../epg-domtest-epg.json?query-target=subtree&target-subtree-class=fvRsDomAtt`) — a real `fvRsDomAtt` object present with `dn: uni/tn-domtest/ap-domtest-ap/epg-domtest-epg/rsdomAtt-[uni/phys-domtest-physdom]`, `tDn: uni/phys-domtest-physdom`, `tCl: physDomP`, `state: formed`.
  7. `terraform destroy` (isolated state) — **`Destroy complete! Resources: 7 destroyed`**.
  8. Cleaned up all Nautobot test objects (VLAN, prefix, VRF, tenant) back to the pre-test baseline.
* **Scope boundary:** this only covers the domain *relation itself* (which domain(s) an EPG deploys into, and with what immediacy settings) — it does not cover per-EPG VMM-specific deployment details beyond `resolution_immediacy`/`deployment_immediacy` (e.g. custom port-group naming, static bindings), which would be a further increment if a real need arises.
* **Known pre-existing data issue surfaced (fixed, 2026-09-04):** tenant `ACI:Sales` had two Bridge Domains both named `DB_BD` (prefixes `10.0.2.0/24` and `10.0.3.0/24`, both created within the same second with an identical `10.x.x.254/24` gateway IP each -- a genuine double-create, not two intentional BDs), which blocked any full-YAML `terraform plan` with a "Duplicate object key" error in `local.bridge_domains`. Fixed by deleting the stray duplicate (`10.0.3.0/24` and its dependent `10.0.3.254/24` gateway IP) via the Nautobot REST API, keeping `10.0.2.0/24` (consistent with the tenant's other sequentially-numbered BDs: `Presales_BD` at `.1`, `Backup_BD` at `.4`). Re-verified: the full generator output now plans cleanly (`Plan: 27 to add, 0 to change, 0 to destroy`, no errors) against the real ACI simulator.
* **MCP-protocol live verification (2026-09-04)**, covering every ACI tool that had only been unit-tested up to this point (`create_vlan_pool`, `create_physical_domain`, `create_aep`, `create_leaf_interface_policy_group` from Phase B; `create_vmm_domain`, `bind_epg_domain` from this phase): started a real server process (`python -m mcp_server.main`, `MCP_TRANSPORT=streamable-http`) pointed at live Nautobot, then drove all six via a real `mcp` SDK client session (`streamable_http_client` + `ClientSession`, not a direct Python function call) — every call returned a normal tool result over the wire (confirmed via the server's own request log, all `POST /mcp` calls `200 OK`), and each write was independently confirmed present in Nautobot before cleanup. Two real, generalizable facts surfaced in the process, not specific to any one tool: (1) most of these `create_*` tools append rather than dedupe-by-name on repeat calls with identical arguments (only `create_aep`/`bind_epg_domain` merge-by-key) — re-running the same call twice (as happened here, by accident) produces duplicate list entries in the JSON Custom Field, the same failure shape as the `ACI:Sales` issue above, just self-inflicted this time; (2) the earlier VMM Domain (Phase D) and EPG-to-Domain (Phase D follow-on) live-verification passes had each cleared their own tenant-side test objects but never reset the shared `ACI-Lab` Location's `aci_fabric_policies` Custom Field itself, leaving a stray `domtest-physdom` entry behind — found and removed during this pass's cleanup. All test objects (the `mcplive-*` tenant/VRF/BD/EPG, and every `aci_fabric_policies` entry) were removed afterward, confirmed back to the pre-test `null` baseline.

## Phase E — Fabric Policies beyond NTP/DNS/SNMP ✅ Complete (2026-09-04)

Revisited the `Platform-Status-and-Pending-Items.md` pending item "Fabric Policies beyond NTP/DNS/SNMP defaults (BGP Route Reflector, COOP Group, ISIS, named Pod Policy Groups/Pod Profiles)" after the user asked to proceed with it.

* **Provider research (real, not assumed):** re-checked `terraform providers schema -json` for `CiscoDevNet/aci` 2.20.0. Unlike Phase C's NTP/DNS/SNMP, **COOP Group Policy and ISIS Domain Policy have real *typed* resources** — `aci_coop_policy` (`type`, `description`) and `aci_isis_domain_policy` (`mtu`, `redistrib_metric`, plus per-level ISIS parameters flattened onto the same resource — no separate child resource needed). Both take **no `parent_dn`**, confirmed via the schema: the provider always targets the one fixed DN itself, since there is only ever one instance of each in a fabric. No typed or `aci_rest_managed`-equivalent resource exists for BGP Route Reflector Policy or named Pod Policy Groups/Pod Profiles — the latter two go through `aci_rest_managed` (`fabricPodPGrp`/`fabricPodP`/`fabricPodS` classes).
* **Live APIC attribute/DN discovery:** confirmed by direct API queries against the real simulator (not guessed) — COOP Group Policy at `uni/fabric/pol-default` (baseline `type=strict`, already `orchestrator:terraform`-annotated from earlier, unrelated work — not something this session caused), ISIS Domain Policy at `uni/fabric/isisDomP-default` (baseline `mtu=1492`, `redistribMetric=60`), BGP Route Reflector Policy at the fixed DN `uni/fabric/bgpInstP-default/rr` (`bgpRRP`, all-computed attributes — no real admin-settable content beyond `descr`), Pod Policy Group container at `uni/fabric/funcprof` (`fabricFuncP`, confirming the `podpgrp-<name>` RN pattern), and the existing default Pod Profile/Selector at `uni/fabric/podprof-default` / `uni/fabric/podprof-default/pods-default-typ-ALL` (`type=ALL`, no Pod Policy Group currently assigned — using implicit fabric defaults).
* **A real, hard scope boundary found, not a design gap:** queried `fabricNode` for `role=spine` — **zero spine nodes exist in this simulator** (`totalCount: 0`), the same root cause as the two existing simulator-blocked items (Physical Access Policy port bindings, L3Out physical attachment). BGP Route Reflector Policy's only meaningful configuration (which spine nodes act as reflectors, via `bgpRRNodePEp` children) is therefore **not implementable against this simulator** — the top-level policy object has no other real content worth managing. Documented as permanently blocked, same class as the two existing items, not attempted further.
* **A second scope boundary chosen deliberately, not simulator-forced:** modifying the *existing* default Pod Selector (`uni/fabric/podprof-default/pods-default-typ-ALL`) to assign it to a named Pod Policy Group would change this shared lab's actual live fabric-wide pod policy in effect — a real operational risk, not a mandatory-singleton-delete risk like Phase C's, but still a live, shared-state side effect worth its own dedicated, carefully-planned increment rather than bundling into this pass. **Named Pod Policy Groups are implemented as standalone objects you can create/manage**; binding one to the live Pod Selector is deferred.
* **Nautobot data model:** same fabric-wide `aci_fabric_policies` JSON Custom Field on `dcim.location` used by Phases B/C/D — extended with `coop`/`isis` (singleton keys, same last-Location-wins semantics as `ntp`/`dns`/`snmp`) and `pod_policy_groups` (list key, aggregated across Locations, same convention as `vlan_pools`/`aeps`/etc., since named Pod Policy Groups are purely additive with no default instance).
* **Generator:** `_build_fabric_and_access_policies()` extended to read `coop`/`isis`/`pod_policy_groups` and emit them under `apic.fabric_policies`, same pass-through convention (no local value validation) as every other key in this function.
* **Terraform:** `aci_coop_policy.this`/`aci_isis_domain_policy.this` (both `count`-gated on the corresponding key being set, no `parent_dn`), `aci_rest_managed.pod_policy_group` (ordinary `for_each`, `dn = "uni/fabric/funcprof/podpgrp-${name}"`, **no `content_on_destroy`** — unlike Phase C's singletons, this object doesn't exist by default, so deleting the DN on destroy is the *correct* behavior here, not a hazard).
* **Unit-tested:** 6 new generator tests (emission, absence-when-unset, singleton last-Location-wins for `coop`/`isis`, aggregation for `pod_policy_groups`) — full suite 37/37 passing.
* **Verified live with a real `apply`+`destroy`, including a deliberately cautious destroy test on the two mandatory singletons:**
  1. Set test `coop`/`isis`/`pod_policy_groups` data on the ACI-Lab Location matching the real live baseline values for `coop`/`isis` (`type=strict`, `mtu=1492`/`redistrib_metric=60`) specifically to minimize risk if destroy behaved unsafely, regenerated `tenants.yaml` — generator correctly emitted all three under `apic.fabric_policies`.
  2. `terraform plan` (isolated state, 3 targeted resources) — **`Plan: 3 to add, 0 to change, 0 to destroy`**.
  3. `terraform apply` — **`Apply complete! Resources: 3 added`** — confirmed live via direct APIC queries (COOP `type=strict`, ISIS `mtu=1492`/`redistribMetric=60`, Pod Policy Group `fabricPodPGrp` present with the correct name).
  4. Destroyed the (safe, purely additive) Pod Policy Group first — clean `Destroy complete! Resources: 1 destroyed`, no concerns since this object has no default instance.
  5. **Destroy-tested COOP Group Policy next, with an immediate independent APIC re-query right after** (the same discipline Phase C's incident should have had from the start): Terraform itself surfaced a provider warning — `"Warning: Resource with class name coopPol cannot be deleted"` — and the live object's `modTs` was completely unchanged from before the test, `type` still `strict`. The provider silently refuses the real APIC delete for this class and only removes the resource from Terraform state.
  6. **Destroy-tested ISIS Domain Policy the same way** — identical protective behavior: `"Warning: Resource with class name isisDomPol and isisLvlComp cannot be deleted"`, live object's `modTs`/`mtu`/`redistribMetric` completely unchanged.
  7. Cleared the ACI-Lab Location's `aci_fabric_policies` Custom Field back to `null`, matching the pre-test baseline.
* **Generalizable lesson (a positive counterpart to Phase C's):** not every ACI object that "can't really be deleted" needs a Phase-C-style `content_on_destroy` safety net — some of the provider's real *typed* resources (confirmed here for `aci_coop_policy`/`aci_isis_domain_policy`) already refuse the underlying delete themselves and emit a warning instead of silently corrupting state. This protection must still never be assumed without testing directly against the real target system — it happens to exist for these two classes in this provider version, not as a general guarantee for every "mandatory singleton" resource type.

---

# Consequences

**Positive:** each phase is independently shippable and independently valuable (Phase A alone closes the gap for security-policy-adjacent tenant configuration; Phase B alone closes the gap for physical fabric staging; Phase C alone closes the gap for POD-wide operational configuration) — sequencing avoids a single large, hard-to-review change. Reusing `ACI-Nautobot-NaC-Architecture.md`'s existing schema work for Phase A item 2 and Phase B item 2 avoids redesigning what's already been thought through.

**Negative / risk:** Phase A items 3-4 (Contracts, L3Out) and all of Phase B require new Nautobot data modeling decisions (custom models vs. Custom Fields vs. Relationships) that ADR-001/019 do not currently answer — each should get a short design note before implementation, not be improvised mid-PR. Phase B's Access Policy schema was drafted against an unverified provider version and must be re-checked, not assumed correct — **item 1's experience confirms this concern was warranted**: the older doc's VRF/BD field names and the real provider's differed in several places. Phase C confirmed a new, generalizable risk: `aci_rest_managed` (and any similar generic REST-managed resource) is unsafe by default against mandatory system singleton objects unless `content_on_destroy` is explicitly set — this was caught only because destroy was actually tested live, not assumed safe from a successful apply.

**No longer out of scope:** Fabric-wide POD policies (NTP/DNS/SNMP) — implemented in Phase C above. COOP Group Policy, ISIS Domain Policy, and named Pod Policy Groups — implemented in Phase E above. BGP Route Reflector node binding and reassigning the live default Pod Selector remain out of scope (the former permanently, by simulator limitation; the latter deliberately deferred — see Phase E).

---

# Phase A Item 1 — Live Verification (2026-07-29)

VRF and Bridge Domain attribute depth (Custom Fields, generator, and Terraform changes) was implemented, then verified against the real ACI simulator once it came back online (it had been unreachable earlier the same day — see Milestone 4/5's findings for this same recurring external-outage class):

- `terraform plan` against the full live tenant set (30 tenants) with no test attributes set: **`Plan: 4 to add, 0 to change, 0 to destroy`** — the 4-to-add is pre-existing, unrelated drift (a `materialization-test-*` debris tenant already known from earlier sessions); critically, **0 to change** confirms the new optional attributes introduce no drift on any already-applied resource when left unset.
- Set `aci_policy_control_enforcement_mode: "unenforced"` on `web-vrf` via a real Nautobot Custom Field write, regenerated the YAML, re-ran `terraform plan`: **`Plan: 4 to add, 1 to change, 0 to destroy`**, with the plan showing exactly `aci_vrf.this["web-tenant/web-vrf"] will be updated in-place`, `policy_control_enforcement_mode = "enforced" -> "unenforced"` — Terraform correctly read the live APIC's current value (`"enforced"`) and proposed exactly the intended change.
- Reverted the test value, regenerated, re-ran `terraform plan`: back to `0 to change`, and `tenants.yaml` matched git HEAD exactly (no leftover diff).

This closes Phase A item 1's live-verification gap noted when it was first implemented (the simulator was down at the time). No `terraform apply` was run — a plan-only proof is sufficient for a non-destructive attribute-depth addition, and applying would have changed live simulator state for a value that was only ever meant as a test, not deliberate configuration.


