---
title: "Platform Status and Pending Items"
description: "Single, current source of truth for what's resolved, what's pending, and what's blocked across the platform -- for AI agents and engineers to check before starting new work."
type: architecture
domain: platform
status: active
tags: [status, pending-items]
owner: platform-engineering-team
last_updated: 2026-09-04
---

# Platform Status and Pending Items

This is the **current, authoritative status tracker** for the Network Platform Engineering Platform. It exists so an AI agent (or engineer) starting a new session can quickly answer: *what's already done, what's known-broken, what's intentionally deferred, and what needs a decision before proceeding?*

> **Supersedes [`Current-State-v1.md`](archive/Current-State-v1.md) for status purposes.** That document is dated 2026-07-04 and describes Platform v1 (Platform API, n8n orchestration, no MCP Server, no Execution Framework) — it predates [ADR-016](../adr/ADR-016-Platform-v2-Replacement-Architecture.md) through [ADR-021](../adr/ADR-021-VXLAN-EVPN-Domain-Expansion.md) entirely and should not be trusted as current. It has been moved to `architecture/archive/` alongside the other superseded Platform v1 documents, not deleted. This document is updated instead, going forward.

> **Per [`knowledge/README.md`](../README.md)'s rule, this vault never contains live infrastructure state** — no live tenant counts, object UUIDs, or point-in-time snapshots. Everything below is a durable fact about the platform's *build status*, not a data export. For live counts/IDs, query Nautobot/GitLab directly.

---

## 1. What's Complete

- **Execution Framework** ([`Execution-Framework.md`](Execution-Framework.md)) — all 6 milestones complete: GitLab CI pipeline (Validate → Policy → Approval → Execution → Verification → Knowledge Capture), Nautobot→NetAsCode generator, MCP Server, and an AI agent (VS Code Copilot Agent) completing a full business operation end-to-end with no manual pipeline triggering.
- **ACI domain coverage** ([ADR-020](../adr/ADR-020-ACI-Domain-Coverage-Expansion.md)) — Tenant/VRF/Bridge-Domain/Subnet, Application Profiles/EPGs, Contracts/Filters/Subjects, L3Out (logical-only), Access Policies (logical-only), fabric-wide POD Policies (NTP/DNS/SNMP, Phase C, no scope limitation), VMM Domain integration (Phase D, 2026-09-04, no scope limitation — live-verified against this lab's real vCenter), EPG-to-Domain binding (Phase D follow-on, 2026-09-04, Physical + VMM, no scope limitation), Fabric Policies beyond NTP/DNS/SNMP (Phase E, 2026-09-04: COOP Group Policy, ISIS Domain Policy, named Pod Policy Groups), and RBAC/Security Domains/Local Users (Phase F, 2026-09-04) all implemented and live-verified against the real ACI simulator.
- **MCP Server tool catalogue**: all 18 tools (`create_tenant`, `create_vrf`, `create_bridge_domain`, `create_epg`, `bind_epg_domain`, `create_contract`, `create_l3out`, `create_vlan_pool`, `create_physical_domain`, `create_aep`, `create_leaf_interface_policy_group`, `create_vmm_domain`, `create_security_domain`, `create_local_user`, `create_evpn_tenant`, `create_evpn_vrf`, `create_evpn_bridge_domain`, `show_status`) are unit-tested and live-verified over the real MCP protocol (2026-09-04: `create_vlan_pool`, `create_physical_domain`, `create_aep`, `create_leaf_interface_policy_group`, `create_vmm_domain`, `bind_epg_domain`, `create_security_domain`, `create_local_user` confirmed via a real `streamable-http` MCP client session against a running server instance pointed at live Nautobot -- all calls returned successfully, verified in Nautobot, then cleaned up).
- **Custom agent persona**: `.github/agents/network-platform-operator.agent.md` — a tool-restricted business-operator persona (MCP tools + read/search only, no terminal access — see §3 for why terminal access was removed).
- **Debris test tenant cleanup (2026-07-31)** — the 28 `materialization-test-*`/`real-tf-test-*`/`real-tf-fail-*` tenants (and their dedicated VRF + Namespace each) accumulated from earlier live-verification sessions were removed via direct Nautobot REST API calls, after confirming zero shared namespaces or other dependents. Only the 7 legitimate tenants remain.
- **Platform-Administration-Guide.md** (repo root) — operational handbook for every running container.
- **EVPN domain expansion, live verification achieved on all 4 real nodes** ([ADR-021](../adr/ADR-021-VXLAN-EVPN-Domain-Expansion.md)) — generator, Terraform module (now including `nxos_bgp`/`nxos_svi_interface`/`nxos_ipv4`), Nautobot Custom Fields, OPA policy (`vxlan_evpn`, tested with real `opa eval`), and 3 MCP tools (`create_evpn_tenant`/`create_evpn_vrf`/`create_evpn_bridge_domain`, live-verified over the real MCP protocol) are all built and verified. **Real `terraform apply` cycles succeeded against `DC1-Leaf` (2026-07-30, including a `destroy`) and `DC1-BGW` (2026-07-31)** on a genuine Nexus 9000v CML lab (`172.30.46.250`) via a jump host inside the lab network — all 4 devices now have real `terraform apply` cycles (`DC2-Leaf`/`DC2-BGW` applied 2026-08-26 during BGP peer/neighbor work, ADR-021 §23). `DC1-BGW`'s earlier blocker turned out to be a simple missing `mgmt0` IP address (not the stuck console pager originally suspected) — fixed via console, then verified with a real `terraform apply`/`plan` (0 diff). This closes ADR-021's original "no live verification possible" gap. A real destroy-safety bug was found in the process and genuinely fixed, then live-verified end-to-end against `DC1-Leaf` (2026-08-26). **Every job in `pipelines/evpn.gitlab-ci.yml` is now real and live-verified against actual hardware, and the file is included from the root `.gitlab-ci.yml`** (2026-08-26) — `terraform_plan`/`terraform_apply`/`ansible_configure` via a jump-host relay mechanism, `pyats_verify` via a plain-Python reimplementation with no pyATS/`genie` dependency (a second jump host was ruled out by this lab's 5-node CML license cap). A real job-name collision with ACI's pipeline was found and fixed before inclusion (EVPN's jobs are now all `evpn_`-prefixed). See §2 for details.

---

## 2. Known Pending Items (need a decision or further work)

| Item | Status | Notes |
|---|---|---|
| EVPN pipeline wiring | **All jobs fixed, live-verified, and included from the root pipeline (2026-08-26)** | The file-transfer extension needed for `terraform_plan`/`apply` (previously concluded blocked, ADR-021 §14) turned out not to be — ADR-021 §17 found the fix: drive both legs of the transfer (CML's external dropfolder → upload; internal dropfolder → jump host pull) from *outside* the jump host, nothing installed on the jump host itself. New scripts `cml-jump-fetch-file.sh`/`.exp`, `cml-terraform-run.sh`, plus a persistent provider mirror at `/home/cisco/tf-mirror` (survives reboots, unlike `/tmp`). Live-verified: a real `terraform plan` against the full generator-driven `platform/terraform/evpn` module succeeded against `DC1-Leaf` (`Plan: 9 to add`). **`ansible_configure` fixed (ADR-021 §18)**: the shared `.ansible_configure` template was itself wrong for this domain (installed `cisco.aci`, not `cisco.nxos`; hardcoded the ACI playbook name). New `cml-provision-py-env.sh` (idempotent, persistent `/home/cisco/py-env`) bundles a portable musl Python + `ansible-core` + collections onto the jump host; `cml-ansible-run.sh` relays the playbook run. Also fixed `nxos_vrf`'s nonexistent `query` state (switched to `nxos_vrf_global`'s `gathered`) and a placeholder inventory. Full live run: `ok=4 changed=0 failed=0 skipped=1`. **`pyats_verify` fixed (ADR-021 §19)**: `genie` (needed transitively by pyATS's core CLI, REST connector, and `unicon`) has no PyPI release combining a working dependency tree with a musllinux wheel — confirmed via direct tracebacks across three different invocation paths, a real upstream packaging bug (`genie==26.7`'s own declared `genie.metaparser` dependency doesn't exist as an installable package at all). A second, glibc-based jump host was considered and rejected: this lab's CML Personal/Community license caps at 5 concurrent nodes, and the 4 real devices + existing jump host already use all 5. Fixed instead with `tests/pyats/evpn/verify_fabric_features.py` — a plain-Python reimplementation of the same check (same logic, no pyATS/`genie` dependency at all), run via new `cml-verify-fabric-run.sh`. `test_evpn_features.py` (the original pyATS version) is left in place, unchanged, as a reference implementation, not run by the pipeline. **Included from the root pipeline (ADR-021 §20)**: found and fixed a real job-name collision first — `pipelines/aci.gitlab-ci.yml` and `pipelines/evpn.gitlab-ci.yml` both defined 9 identically-named jobs (`generate_nac`, `terraform_plan`, etc.), which GitLab CI's `include:` merge would have silently resolved by having the last-included file's jobs completely override the first's, breaking ACI invisibly. Fixed by prefixing every EVPN job with `evpn_`, confirmed programmatically (19 unique jobs, zero overlap) before adding `pipelines/evpn.gitlab-ci.yml` to `.gitlab-ci.yml`'s `include:` list. A post-wiring audit (ADR-021 §21) also found and fixed a missing `resource_group` lock — all 5 jobs that touch the jump host now share one lock, since CML's console is single-session and any two of them running concurrently (from any pipeline run) could conflict, not just a Terraform-state race. **Only remaining item**: this hasn't been confirmed via an actual live GitLab pipeline run triggered by a push yet -- Terraform state persistence and the TF_VAR_bgp_asn placeholder gap are both now fixed (see the rows below). |
| EVPN Terraform state not persisted between pipeline runs | **Fixed and live-verified end-to-end (2026-08-26)** | `cml-terraform-run.sh` now persists `terraform.tfstate` at a stable jump-host path (`/home/cisco/tf-state/<slug>`, matching the existing `tf-mirror`/`py-env` persistence pattern) instead of wiping it every run, and `terraform_apply` executes the *exact* saved plan from `terraform_plan` (via a new reverse file-transfer mechanism — `cml-jump-push-file.exp`/`.sh` + `cml-fetch-from-jumphost.sh` — that shuttles the `tfplan` file back from the jump host as a real GitLab artifact). Live-verified against real `DC1-Leaf` hardware: plan → apply (9 resources added) → a second, independent plan correctly restored state and reported "No changes" (ADR-021 §22). A real, unrelated `expect` buffer-truncation bug (default `match_max` 2000 bytes silently dropping output on large terraform init/plan runs) was found and fixed in all three relay `expect` scripts during this verification. |
| `nxos_feature` resource does not actually revert device state on `terraform destroy` | **Fixed and live-verified (2026-08-26)** | Live-tested on `DC1-Leaf`: `terraform destroy` originally reported `Resources: 1 destroyed`, but independent NX-API queries showed `bgp`/`interface-vlan` still `enabled` on the real device. Confirmed via the real `CiscoDevNet/nxos` v0.13.1 provider schema that `nxos_feature` has no `content_on_destroy`-equivalent attribute. Genuinely fixed (ADR-021 §15) with a `null_resource` carrying a destroy-time `local-exec` provisioner that issues the real NX-API revert commands directly (credentials threaded through the null_resource's `triggers`, since destroy-time provisioners can't reference `var.*` directly). `lifecycle { prevent_destroy = true }` removed from `nxos_feature.fabric`. **Live-verified end-to-end (2026-08-26, ADR-021 §16)**: a real `apply` → `plan` (0 diff) → `destroy` cycle against `DC1-Leaf`, with an independent NX-API `show feature` query immediately after confirming `bgp`/`interface-vlan`/`nve` genuinely `disabled` — the destroy-time revert works. Two real bugs surfaced and were fixed during this verification pass: (1) the provisioner's original `curl`-based command failed on the jump host, which only has busybox `wget` — rewritten to use `wget` with a manually-built Basic auth header and `--post-file`, matching this lab's existing wget convention; (2) `pipelines/scripts/cml-jump-relay.exp`/`.sh` assumed the console is always already at a shell prompt, which is false for a cold-booted jump host — fixed to send a wake-up keypress and handle the resulting OS login prompt. |
| EVPN BGP peer/neighbor configuration | **Fixed and live-verified fabric-wide (2026-08-26)** | Implemented via a new Device.evpn_bgp_peers JSON Custom Field (peer_ip/remote_asn/description per device) sourced from real, live-queried interface addressing on all 4 devices, plus a new var.device_name so main.tf resolves each device's own BGP ASN/peers from fabric.yaml (closing the TF_VAR_bgp_asn placeholder gap too). A partially-broken day-0 reference design was found already configured on 3 of the 4 devices (site-shared ASN, loopback-based iBGP that never established -- no underlay IGP exists) and corrected to use directly-connected interface IPs instead, matching the one link already proven to work (the BGW-to-BGW multi-site eBGP session). Live-verified: all 5 real BGP/EVPN sessions across the fabric are Up and exchanging routes (ADR-021 SS23). |
| EVPN SVI IP addressing | **Implemented and apply-verified against all 4 real devices (2026-08-26)** | `nxos_ipv4` resource added to `platform/terraform/evpn/main.tf` (ADR-021 §8), schema-confirmed via `terraform plan` (2026-07-31), then genuinely `apply`-verified against real DC1-Leaf/DC1-BGW/DC2-Leaf/DC2-BGW hardware during the state-persistence (§22) and BGP peer/neighbor (§23) live-verification passes -- each real apply included `nxos_ipv4.fabric` among its 9 created resources. |
| Custom agent `PreToolUse` hook mechanism | **Unresolved root cause** | A hook meant to block destructive terminal commands (`.github/agents/scripts/block-destructive-commands.sh`) passed when tested in isolation (piping JSON directly into the script) but did **not** fire during a live agent run via `runSubagent` — the agent executed `docker compose down` unblocked, causing a real incident (recovered, zero data loss). Root cause not conclusively diagnosed (candidates: hooks may not apply to `runSubagent`-invoked agents, `${workspaceFolder}` may not resolve in the hook `command:`, or the payload shape assumption may be wrong). **Fix applied**: `execute` (terminal access) removed from the agent's tool list entirely — a real, structural guarantee, not a hook. Do not re-add terminal access to that agent based on the hook alone; verify with a harmless canary command first if this is revisited. |
| LangGraph reasoning / multi-domain business-operation layer | **Not started** | [ADR-019](../adr/ADR-019-Three-Truths-Principle.md) explicitly leaves this as future work — a layer that sequences multiple MCP tool calls across domains for business-intent-level operations (e.g. spanning ACI + EVPN + Fortinet in one request). Today, sequencing is done ad hoc by whichever AI agent/persona is active. |
| Claude Desktop as a second MCP client | **Not started, optional** | VS Code Copilot Agent already proved the MCP mechanism (Milestone 6). Adding Claude Desktop is a smaller, lower-priority follow-on, not a blocker for anything else. |
| Physical Access Policy interface/port bindings (ACI) | **Permanently blocked by simulator, not a gap** | Confirmed via direct APIC API queries: this lab's ACI simulator has zero real leaf/spine interface data (no `l1PhysIf` objects). Logical-only scope (VLAN Pools, Physical/VMM Domains, AEPs, Leaf Interface Policy Groups) is the ceiling until real hardware or a more capable simulator is available. |
| L3Out physical attachment (routed interfaces, OSPF/BGP peering) | **Permanently blocked by simulator, not a gap** | Same root cause as the item above — no real leaf/spine interface data exists on this simulator. L3Out is logical-only (object, VRF relation, External EPG, subnets) per ADR-020 Phase A item 4. |
| Fabric Policies beyond NTP/DNS/SNMP defaults (BGP Route Reflector, COOP Group, ISIS, named Pod Policy Groups/Pod Profiles) | **COOP/ISIS/named Pod Policy Groups complete (ADR-020 Phase E, 2026-09-04)** | Live-verified against the real ACI simulator, including a deliberately cautious destroy test on both mandatory singletons (COOP Group Policy, ISIS Domain Policy) -- the provider itself refuses the real delete for these two typed resources (`"cannot be deleted"` warning), leaving the live objects untouched. **BGP Route Reflector node binding is permanently blocked by this simulator** (zero real spine nodes exist, confirmed via direct APIC query -- same root cause as the two other simulator-blocked items). **Reassigning the live default Pod Selector to a named Pod Policy Group is deliberately deferred** (a real, shared live-fabric side effect, not a simulator limitation -- needs its own carefully-planned increment). Multi-POD fabrics still need named (non-default) Pod Profiles/Selectors bound via Pod Policy Groups -- not designed. |
| VMM Domain integration (VMware/Kubernetes) | **VMware complete (ADR-020 Phase D, 2026-09-04)** | Live-verified against this lab's real vCenter (`netvcenter.sc.gr`) and the real ACI simulator — no scope limitation, unlike the two simulator-blocked items above. Kubernetes/other VMM vendors still not started (would need a different controller integration this lab doesn't have). |
| EPG-to-Domain binding (Physical + VMM) | **Complete (ADR-020 Phase D follow-on, 2026-09-04)** | Live-verified against the real ACI simulator (`fvRsDomAtt` confirmed via direct APIC query). `aci_application_epg.relation_to_domains` + new `aci_epg_domains` JSON Custom Field on the EPG's VLAN object + `bind_epg_domain` MCP tool. Surfaced and fixed a real gap during verification: the Custom Field itself hadn't been defined in Nautobot's schema yet (writes were silently dropped, no error) — see ADR-020 for the fix. |
| ACI:Sales duplicate `DB_BD` Bridge Domain names | **Fixed (2026-09-04)** | Two Bridge Domains both named `DB_BD` (prefixes `10.0.2.0/24`/`10.0.3.0/24`, each with an identical `.254/24` gateway IP — a genuine double-create) blocked any full-YAML `terraform plan` with a "Duplicate object key" error in `local.bridge_domains`. Deleted the stray duplicate (`10.0.3.0/24` + its dependent gateway IP) via the Nautobot REST API. Re-verified: full generator output now plans cleanly (`Plan: 27 to add, 0 to change, 0 to destroy`, no errors). |
| Multi-Site / Nexus Dashboard Orchestrator (NDO) | **Not started, out of scope** | Never designed or discussed — this platform targets single-fabric ACI via APIC directly, not cross-site NDO policy templates. |
| RBAC / Security Domains / Local Users, Syslog/Fault/Health monitoring policies | **RBAC/Security Domains/Local Users complete (ADR-020 Phase F, 2026-09-04)** | Live-verified against the real ACI simulator, including over the real MCP protocol. Local User passwords never flow through Nautobot or the MCP layer -- supplied at `terraform apply` time via the sensitive `local_user_passwords` Terraform variable, same boundary as Phase D's VMM Controller credentials. **Syslog/Fault/Health monitoring policies remain not started** -- a genuinely separate sub-area of APIC platform administration, not attempted in this pass. |

---

## 3. Operational Hazards to Remember

These are hard-won lessons from real incidents this platform has hit — repeated here (not just in agent memory) so any reader, human or AI, sees them before touching infrastructure.

1. **Never run `docker compose down` (even with a matching `-p <project>`) from a subdirectory whose compose file is one piece of the shared `infra-automation-lab` project.** Compose reconciles *all* resources sharing that project label, not just the current file's services — this has caused two full-stack outages in this lab's history (recovered both times with zero data loss, since `down` without `-v` never deletes named volumes). Use the root `docker/docker-compose.yml` + `--no-deps <service>` for single-service work — see [`Platform-Administration-Guide.md`](../../Platform-Administration-Guide.md) §1.2.
2. **When recreating any container after an incident, cross-check GitLab's stored CI/CD project variables for that service's real credentials** before choosing placeholder values — a MinIO recovery once used different placeholder credentials than GitLab CI expected, silently breaking the `knowledge_capture` pipeline job until diagnosed.
3. **Never trust a safety hook (or any enforcement mechanism) just because it passes in isolated testing.** A `PreToolUse` hook that correctly denied a command when tested by piping JSON directly into the script completely failed to fire during a real agent run. Validate any new hook with a harmless canary command first, in the real runtime, before relying on it for anything destructive.
4. **The generator bundles ALL tenants/objects for a domain into one YAML file.** A single bad object elsewhere (a policy-violating name, a duplicate VRF) can fail `policy_check`/`terraform_plan` for the *entire* pipeline, even for changes about something unrelated. When a pipeline fails, check whether the failure is actually about what you changed before assuming a platform bug.
5. **Any generic "manage any object" Terraform resource (`aci_rest_managed` and equivalents in other providers) defaults to deleting the entire target DN/object on destroy, not just the attributes/children that resource added.** Confirmed live in ADR-020 Phase C: destroying `aci_rest_managed` resources targeting ACI's mandatory system singleton objects (`uni/fabric/time-default`, `uni/fabric/dnsp-default`, `uni/fabric/snmppol-default`) actually deleted them from the real simulator, requiring manual recreation. Fix: always set `content_on_destroy` explicitly to safe/original content when targeting a singleton object with a generic REST-managed resource — never rely on the default destroy behavior. Only caught because destroy was actually tested live, not assumed safe after a successful apply.

---

## 4. How to Keep This Doc Useful

- Update this file whenever a pending item is resolved, a new one is discovered, or a real incident happens — move resolved items out of §2 (a one-line mention in §1 or a git history reference is enough; don't let this file grow into a changelog).
- Do not add live infrastructure data here (tenant counts, object IDs, pipeline numbers) — those go stale immediately and belong in Nautobot/GitLab themselves, not this vault.
- If this file conflicts with a source document (an ADR, `Execution-Framework.md`), the source document wins for design/architecture questions — this file is for status and pending work only.
