# Nested git repo — handle as submodule or separate decision
lab/docker/nautobot/

# Vault unseal keys — never commit
lab/docker/vault/state/*.txt
lab/docker/vault/state/*.json

# Terraform
**/.terraform/
*.tfstate
*.tfstate.backup
.terraform.lock.hcl

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
.pytest_cache/

# macOS / Zone.Identifier artifacts
*.Zone.Identifier
.DS_Store

# Environment / secrets — never commit
*.env
!*.example.env
!creds.example.env

# Generator output — produced by generate_aci.py, not source
platform/netascode/aci/tenants.yaml

# Terraform variable files with real credentials
terraform.tfvars
*.auto.tfvars
