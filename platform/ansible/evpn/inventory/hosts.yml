---
all:
  children:
    evpn:
      hosts:
        # ADR-021: no Nexus 9Kv (or equivalent) simulator exists in this lab
        # yet -- this inventory entry is a placeholder for when one is stood
        # up. cisco.nxos connects over SSH/NX-API depending on module.
        leaf-a:
          ansible_host: "{{ nxos_host | default('CHANGE-ME-no-device-yet') }}"
          ansible_python_interpreter: "{{ ansible_playbook_python }}"
